import * as React from "react";

import { Layout } from "../components/layout";

export default function IndexPage() {
  return <Layout>
    <h1 className="m-5 text-3xl font-bold underline">
      Hello world!
    </h1>
    <p>Something else</p>
  </Layout>;
}
